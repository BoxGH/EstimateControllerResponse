@model IEnumerable<Tj.ResponseTimer.Models.ForTable>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PagePath)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PagePath2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PagePath3)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PagePath)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PagePath2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PagePath3)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
        </td>
    </tr>
}
</table>
